{% extends 'base.html.twig' %}

{% block title %}Hello RedirectionControlController!{% endblock %}

{% block body %}
    <style>
        /* Add custom styles for this template */
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
            background-color: #fff; /* Set a white background */
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .btn-custom {
            background-color: #007BFF; /* Change button color to blue */
            color: #fff; /* Set button text color to white */
            border: none; /* Remove button border */
            padding: 10px 20px; /* Add some padding for better button appearance */
            border-radius: 3px;
            cursor: pointer;
        }

        .btn-custom:hover {
            background-color: #0056b3; /* Change button color on hover */
        }
    </style>

    <div class="d-flex justify-content-center align-items-center" style="height: 100vh;">
        <div class="text-center">
            <h1 style="color: #007BFF;">Bienvenue dans la page de demande de remboursement de frais</h1>
            <h2>Vous êtes ?</h2>
            <div class="button-container">
                <a href="{{ path('app_login') }}" class="btn btn-primary">Employé</a>
                <a href="{{ path('app_login') }}" class="btn btn-success">Comptable</a>
            </div>
        </div>
    </div>

{% endblock %}

